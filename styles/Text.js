import Color from "./Color"

const h1 = {
    fontSize: 34,
    fontWeight: 'bold'
}

const text_app_name = {
    fontSize: 34,
    fontWeight: 'bold',
    color: Color.color_text_primary
}

const text_boarding = {
    fontSize: 20,
    fontWeight: 'bold',
    color: Color.color_text_primary
}

const text_button = {
    fontSize: 18,
    fontWeight: 'bold',
    color: Color.color_text_button
}

const text_title_auth = {
    fontSize: 24,
    fontWeight: 'bold',
    color: Color.color_text_primary,
}

const text_guide = {
    fontSize: 16,
    fontWeight: '700',
    color: Color.color_text_place_holder
}

const input = {
    fontSize: 16,
    fontWeight: '500',
    color: 'black'
}

const text_button_outline = {
    fontSize: 18,
    fontWeight: 'bold',
    color: Color.color_text_primary
}

const TextStyle = {
    h1,
    text_app_name,
    text_boarding,
    text_button,
    text_title_auth,
    text_guide,
    input,
    text_button_outline
}

export default TextStyle